cmake_minimum_required(VERSION 3.4.1)
project(entry)

set(NATIVE_ROOT ${OHOS_SDK_NATIVE}/sysroot)

add_library(entry SHARED 
    napi_init.cpp 
    jsoncpp.cpp)

target_include_directories(entry PRIVATE
    ${NATIVE_ROOT}/usr/include
    ${NATIVE_ROOT}/usr/include/hilog
    ${NATIVE_ROOT}/usr/include/hiappevent
    ${CMAKE_CURRENT_SOURCE_DIR}/json)

target_link_libraries(entry PUBLIC
    libace_napi.z.so
    libhilog_ndk.z.so
    libhiappevent_ndk.z.so)