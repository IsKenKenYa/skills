# 签约代扣接口索引

## 客户端拉起签约

| 客户端类型 | 说明 | 参考资源 |
|---|---|---|
| 标准 HarmonyOS App | 使用 `paymentService.requestContract(...)` | [标准App-客户端签约示例](标准App-客户端签约示例.md) |
| ASCF 元服务 | 使用 `has.requestContract(...)` | [ASCF-客户端签约示例](ASCF-客户端签约示例.md) |
| 客户端配置 | 基础配置检查项与签约前核对项 | [客户端配置检查项](客户端配置检查项.md) |

## 服务端预签约

| 语言 | 说明 | 参考资源 |
|---|---|---|
| Java SDK | 调用 `/api/v2/contract/presign/app` 并返回 `buildContractStr(...)` 结果 | [Java-服务端预签约示例](Java-服务端预签约示例.md) |
| 服务端配置 | `appId`、`mercNo`、`authId`、`planId`、回调地址检查 | [服务端配置检查项](服务端配置检查项.md) |
| 其他语言 | 优先参考通用签名规则和官方接口说明 | [签名与验签规则](../../../references/4-通用规则/接入指南/签名与验签规则.md) |

## 回调处理

| 场景 | 说明 | 参考资源 |
|---|---|---|
| 签约结果回调 | 先验签后处理签约状态 | [回调通知处理](../../../references/4-通用规则/接入指南/回调通知处理.md) |
| 后续代扣结果 | 复用相同的验签与幂等规则 | [回调通知处理](../../../references/4-通用规则/接入指南/回调通知处理.md) |

## 说明

- 若用户问的是“签约为什么拉不起”，优先看客户端配置与 `contractStr` 来源。
- 若用户问的是“预签约接口报错”，优先看服务端配置、`planId` 和签名规则。
